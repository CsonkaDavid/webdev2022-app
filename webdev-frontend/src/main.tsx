import React from 'react'
import ReactDOM from 'react-dom/client'
import NavigationRouter from './components/nav/NavigationRouter'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <NavigationRouter>
    </NavigationRouter>
  </React.StrictMode>
)
